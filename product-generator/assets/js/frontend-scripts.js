(function ($) {
  $(document).ready(function(){ 
    var prgen = $(".prgenapp").closest('.entry-content').removeClass('entry-content').addClass('entry-content-custom');
    //console.log(prgen.find('.entry-content').length);
  });
})(jQuery);